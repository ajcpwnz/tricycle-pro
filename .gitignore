node_modules/
*.log
.DS_Store

# Tricycle Pro lock (generated by init/update)
.tricycle.lock

# Dogfood: installed copies live here but source-of-truth is core/ and modules/
# Ignore everything tricycle init installs so it doesn't duplicate source files in git
.claude/
.specify/
.mcp.json

# Keep tricycle.config.yml tracked (it's the dogfood config for this repo)
# Keep specs/ tracked (feature specs created during dogfooding)
